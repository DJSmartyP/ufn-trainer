UFN TRAINER — Station colour correction

Upload these files over the existing files in the repository root.

Changes:
- Captain accent changed to command gold.
- Engineering accent changed to hazard orange.
- All existing station-colour treatments automatically inherit the corrected colours.
- Cache key bumped so browsers load the updated stylesheet immediately.
